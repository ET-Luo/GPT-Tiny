name: gpt-tiny
channels:
  - pytorch
  - nvidia
  - conda-forge

dependencies:
  - python=3.11
  - pytorch>=2.1.0
  - pytorch-cuda=12.1
  - numpy>=1.24.0
  - pip
  - pip:
      - transformers>=4.36.0
      - tiktoken>=0.5.0
      - tqdm
      - wandb
      - datasets

